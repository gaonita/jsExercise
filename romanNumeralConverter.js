// Convert the given number into a roman numeral.
// All roman numerals answers should be provided in upper-case.

let roman = {
    'I' : 1,
    'V' : 5,
    'X' : 10,
    'L' : 50,
    'C' : 100,
    'D' : 500,
    'M' : 1000
};

let arr = roman.keys;
console.log(roman.C);

console.log(arr);

function convertToRoman(num) {
    //Check how big the num is
    //Check if you can divide with key value number in this object
    //

    // num % roman.

    return num;
}

convertToRoman(36);
